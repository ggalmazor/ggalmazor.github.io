<!doctype html>
<html lang="{{ site.locale }}">
<head>
    {% render "head", metadata: site.metadata, title: data.title %}
</head>
<body class="post">
{% render "post_navbar", metadata: site.metadata, resource: resource %}

{% render "post_title" data: resource.data %}

<main class="w-100 mw7 center">
    {{ content }}
</main>

{% render "footer", metadata: site.metadata %}
</body>
</html>

